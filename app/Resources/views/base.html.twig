<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="{{ asset('build/main.css') }}">
        <link rel="stylesheet" href="{{ asset('build/base.css') }}">
        <link rel="stylesheet" href="{{ asset('build/style.css') }}">
        {% block stylesheets %}{% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>

    <body>
    <!-- Top menu -->

    <nav class="navbar navbar-default navbar-expand-lg py-md-2">
        <a class="navbar-brand" href="#">Art Hunter</a>
        <button class="navbar-toggler custom-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="navbar-collapse collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('museum') }}">Musée</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="{{ path('pokedex') }}">Ma Collection</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('run') }}">Mes Badges</a>
                </li>
            </ul>
        </div>
    </nav>
        {% block body %}{% endblock %}
        <script src="{{ asset('build/app.js') }}"></script>
        <script src="{{ asset('build/demo.js') }}"></script>
        <script src="{{ asset('build/app.imagesloaded.pkgd.min.js') }}"></script>
        <script src="{{ asset('build/TweenMax.min.js') }}"></script>

        {% block javascripts %}{% endblock %}
    </body>
</html>
